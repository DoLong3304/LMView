# ─────────────────────────────────────────────────────────────────────────────
# AI Orchestration — heavy ML / LLM dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Installed ONLY in the ai-service image (docker/ai-service/Dockerfile).
# The backend FastAPI gateway does NOT install these — it proxies chat
# requests to ``ai-service`` via HTTP instead. This keeps the gateway
# image small (~500 MB vs ~3 GB) and avoids pulling torch / transformers
# / vader into every backend pod.
#
# If you need to run the AI pipeline embedded in the backend (the legacy
# ``AI_SERVICE_EMBEDDED=true`` mode), install this file on top of
# ``requirements.txt`` in the backend image.

# LLM proxy / router
litellm>=1.52.0

# RAG embeddings (sentence-transformers pulls torch ~2GB)
sentence-transformers>=2.2.0
numpy>=1.24.0

# LangGraph orchestration framework
langgraph>=1.2.0
langchain-core>=1.0.0

# DashScope (Qwen / Alibaba Cloud) provider
dashscope>=1.25.0

# Additional AI service deps
anthropic>=0.49.0
openai>=1.55.0
